/**
 * Note: Multer has been uninstalled.
 * File uploads are now processed natively by fastify-multipart locally in route controllers.
 */
export const uploadConfig = {};
export default uploadConfig;