/**
 * Note: Multer has been uninstalled alongside Express.
 * Fastify handles multipart/form-data natively via @fastify/multipart and request.file()
 */
export const uploadMiddleware = {};
export default uploadMiddleware;
